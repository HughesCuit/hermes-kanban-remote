name: hermes-agent-cluster
version: 3.1.0
description: "Distributed Kanban cluster extension — multi-node task coordination via Python FastAPI server"
author: "Heventure Group"
hooks:
  - on_session_start
  - on_session_end
pip_dependencies:
  - fastapi
  - uvicorn
  - pydantic
  - httpx
